export interface ILink {
  linkId: string;
  originalLink: string;
  shortLink: string;
  clicks: number;
  lifeTime: string;
  createdAt: string;
  isActive: boolean;
  userId: string;
  userEmail: string;
}
